package ir.barasm.presenter.process;

import ir.barasm.presenter.concurrent.Agent;

public class ProcessorAgent extends Agent {

    public ProcessorAgent(Processor processor) {
        super(processor);
    }
}
