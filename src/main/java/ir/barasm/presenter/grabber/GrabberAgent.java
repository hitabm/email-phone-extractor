package ir.barasm.presenter.grabber;

import ir.barasm.presenter.concurrent.Agent;

public class GrabberAgent extends Agent {

    public GrabberAgent(Grabber grabber) {
        super(grabber);
    }
}
